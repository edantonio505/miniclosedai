"""MiniClosedAI client libraries — kept out of the gateway runtime.

`xbench_client` is the canonical client for the four benchmark-friendly
routes (clone, auto-register, params-merge, in-flight 409). Import as:

    from clients.xbench_client import XBenchClient, GenerationInFlight, cloned_bots
"""
